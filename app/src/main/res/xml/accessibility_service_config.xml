<?xml version="1.0" encoding="utf-8"?>
<!--
    Configuración del AccessibilityService.

    eventTypes: qué tipos de eventos escuchar
    - typeWindowStateChanged: cuando cambia la pantalla/actividad activa
    - typeViewClicked: cuando el usuario toca algo

    Sin packageNames: el OS nos manda eventos de TODAS las apps. El filtrado
    real lo hacemos en código (onAccessibilityEvent). En modo "solo SC Pack"
    descartamos los eventos externos; en modo global los reportamos en gris.
    Esto evita la quirk de Android donde cambiar packageNames en runtime
    requiere desactivar/reactivar el servicio para que surta efecto.

    flags:
    - flagReportViewIds: incluye el ID de recurso de cada vista
    - flagRetrieveInteractiveWindows: acceso a TODAS las ventanas del sistema
-->
<accessibility-service
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeViewClicked"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagReportViewIds|flagRetrieveInteractiveWindows"
    android:canRetrieveWindowContent="true"
    android:notificationTimeout="100"
    android:description="@string/accessibility_service_description" />
