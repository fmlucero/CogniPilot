<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permisos core -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- HU-57: osmdroid usa el estado de red para gestionar la descarga/cache de tiles OSM -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Necesario para mostrar la notif "Horario actualizado" en Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- HU-41: GPS reporting al back para el mapa de flota.
         FINE para precisión metros; COARSE como fallback (red celular). -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LogisticsMonitor">

        <!-- Preload de fuentes Google (Barlow Condensed) — evita parpadeo -->
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <!-- HU-03: SplashActivity es el nuevo launcher; decide login vs main -->
        <activity
            android:name=".ui.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.LogisticsMonitor"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- HU-03: pantalla de login -->
        <activity
            android:name=".ui.LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.LogisticsMonitor"
            android:label="@string/app_name" />

        <!-- Actividad principal (ahora solo accesible con sesión) -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:label="@string/app_name" />

        <!-- Servicio de accesibilidad: NÚCLEO del monitoreo -->
        <service
            android:name=".LogisticsAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true"
            android:label="@string/accessibility_service_label">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Servicio foreground para notificación persistente + reporting GPS (HU-41).
             foregroundServiceType=dataSync|location requerido para que el service
             pueda consumir ubicación en background (Android 10+). -->
        <service
            android:name=".LogisticsMonitoringService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync|location" />

    </application>

</manifest>
